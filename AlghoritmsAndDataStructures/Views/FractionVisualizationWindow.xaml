<Window x:Class="AlghoritmsAndDataStructures.Views.FractionVisualizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Визуализация дроби M/N" 
        Height="600" Width="850"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <Style x:Key="CaptionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource CaptionForeground}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3A3A5A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Slider">
            <Setter Property="Background" Value="{DynamicResource TileBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource TileBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="Margin" Value="5,0,5,0"/>
            <Setter Property="IsSnapToTickEnabled" Value="False"/>
            <Setter Property="TickFrequency" Value="1"/>
        </Style>

        <!-- Стиль для выравнивания текста в нижней панели -->
        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MainTextForeground}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ValueTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Yellow"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="30"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource MainBackground}"
            BorderBrush="{DynamicResource WindowBorderBrush}"
            BorderThickness="{DynamicResource WindowBorderThickness}"
            CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="15" ShadowDepth="0"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Border Grid.Row="0"
                    Background="{DynamicResource CaptionBackground}"
                    MouseLeftButtonDown="CaptionBorder_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Визуализация дроби M/N" FontSize="20" FontWeight="Bold"
                               Foreground="{DynamicResource CaptionForeground}"
                               VerticalAlignment="Center" HorizontalAlignment="Left"
                               Margin="20,0,0,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button Style="{StaticResource CaptionButtonStyle}" Click="MinimizeButton_Click">
                            <TextBlock Text="─" FontSize="24"/>
                        </Button>
                        <Button Style="{StaticResource CaptionButtonStyle}" Click="MaximizeButton_Click">
                            <TextBlock Text="☐" FontSize="22"/>
                        </Button>
                        <Button Style="{StaticResource CaptionButtonStyle}" Click="CloseButton_Click">
                            <TextBlock Text="✕" FontSize="20"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Основное содержимое -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Canvas для круга -->
                <Canvas Grid.Column="0" x:Name="DrawingCanvas" Background="{DynamicResource TileBackground}" 
                        Width="400" Height="400" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <!-- Панель управления справа -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0" Width="250">
                    <!-- M -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
                        <TextBlock Text="M =" Foreground="{DynamicResource MainTextForeground}" FontWeight="Bold" VerticalAlignment="Center" Width="30"/>
                        <Slider x:Name="SliderM" Minimum="0" Maximum="50" Value="10" TickFrequency="1" IsSnapToTickEnabled="True"
                                ValueChanged="SliderM_ValueChanged" ToolTip="M"/>
                        <TextBlock x:Name="TextM" Text="10" Foreground="{DynamicResource ResultForeground}" FontSize="16" FontWeight="Bold" Width="30" TextAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- N -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,15">
                        <TextBlock Text="N =" Foreground="{DynamicResource MainTextForeground}" FontWeight="Bold" VerticalAlignment="Center" Width="30"/>
                        <Slider x:Name="SliderN" Minimum="1" Maximum="20" Value="3" TickFrequency="1" IsSnapToTickEnabled="True"
                                ValueChanged="SliderN_ValueChanged" ToolTip="N (не может быть 0)"/>
                        <TextBlock x:Name="TextN" Text="3" Foreground="{DynamicResource ResultForeground}" FontSize="16" FontWeight="Bold" Width="30" TextAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Результаты (расширенный блок) -->
                    <Border Background="{DynamicResource TileBackground}" BorderBrush="{DynamicResource TileBorderBrush}" 
                            BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,0" MinWidth="220">
                        <StackPanel>
                            <TextBlock Text="Результаты:" Foreground="{DynamicResource MainTextForeground}" FontWeight="Bold" FontSize="14"/>
                            <TextBlock x:Name="ResultIntegerPart" Foreground="{DynamicResource ResultForeground}" FontSize="16" Margin="0,5,0,0" TextWrapping="Wrap"/>
                            <TextBlock x:Name="ResultFractionPart" Foreground="{DynamicResource ResultForeground}" FontSize="16" TextWrapping="Wrap"/>
                            <TextBlock x:Name="ResultIntegerLastDigit" Foreground="{DynamicResource ResultForeground}" FontSize="16" TextWrapping="Wrap"/>
                            <TextBlock x:Name="ResultFractionFirstDigit" Foreground="{DynamicResource ResultForeground}" FontSize="16" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- Нижняя панель (выровнена по центру) -->
            <Border Grid.Row="2" Background="{DynamicResource CaptionBackground}" Padding="15,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Левая часть -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,20,0">
                        <TextBlock Text="Младшая цифра целой части: " Style="{StaticResource LabelTextStyle}"/>
                        <TextBlock x:Name="DisplayIntegerLastDigit" Style="{StaticResource ValueTextStyle}" Foreground="Yellow"/>
                    </StackPanel>

                    <!-- Разделитель -->
                    <TextBlock Grid.Column="1" Text="|" Foreground="{DynamicResource TileBorderBrush}" FontSize="24" VerticalAlignment="Center" Margin="0,0,20,0"/>

                    <!-- Правая часть -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left">
                        <TextBlock Text="Старшая цифра дробной части: " Style="{StaticResource LabelTextStyle}"/>
                        <TextBlock x:Name="DisplayFractionFirstDigit" Style="{StaticResource ValueTextStyle}" Foreground="Cyan"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>